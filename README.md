# c-project
This project implements a file and image compression system in C++ that reduces the size of data files (text, binary, and images) without losing essential information. The system uses compression algorithms such as Huffman Coding, Run-Length Encoding (RLE), or LZW (Lempel-Ziv-Welch) to efficiently encode data and minimize storage space.  
